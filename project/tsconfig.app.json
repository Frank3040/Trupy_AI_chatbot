
{
  // Configuración específica de la aplicación (extiende tsconfig.json)
  "extends": "./tsconfig.json",

  "compilerOptions": {
    /* Carpeta de salida para el build de Vite */
    "outDir": "dist",

    /* Tipos globales que aporta Vite (import.meta.env, etc.) */
    "types": ["vite/client"]
  },

  /* Solo incluimos el código fuente de la app */
  "include": ["src"]
}
